# OpenAI API Key for advanced topic extraction (optional)
OPENAI_API_KEY=your_openai_api_key_here

# Topic extraction method preference (optional)
# Options: openai,keybert,bertopic,nltk
TOPIC_EXTRACTION_METHODS=openai,keybert,bertopic,nltk

# Installation Notes:
# If you encounter "Failed building wheel for hdbscan" error with BERTopic:
#   Option 1: Use requirements-minimal.txt (excludes BERTopic)
#   Option 2: Install via conda: conda install -c conda-forge hdbscan
#   Option 3: Install build tools (see INSTALLATION.md)
#
# Windows: Install Microsoft Visual C++ Build Tools
# Linux: sudo apt-get install gcc python3-dev
# Docker: RUN apt-get update && apt-get -y install gcc python3-dev
#
# The system works excellently with just OpenAI + KeyBERT + NLTK
# BERTopic is optional for advanced topic modeling
